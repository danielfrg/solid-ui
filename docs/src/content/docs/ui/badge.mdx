---
title: Badge
description: Styled badge component for displaying short status text.
---

import StyledDemoBlock from "../../../components/styled-demo-block.astro"
import demoCode from "../../../demos/styled/badge.tsx?raw"
import {
  BadgeVariants,
  BadgeWithIconLeft,
  BadgeWithIconRight,
  BadgeWithSpinner,
  BadgeAsLink,
  BadgeLongText,
  BadgeCustomColors,
} from "../../../demos/styled/badge"

# Badge

## Usage

Add the theme to your CSS:

```css
@import "tailwindcss";
@import "@danielfrg/solid-ui/theme.css";
```

Import and use the `Badge` component:

```tsx
import { Badge } from "@danielfrg/solid-ui/badge"
;<Badge variant="outline">Badge</Badge>
```

## Demo

### Variants

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeVariants client:load />
</StyledDemoBlock>

### With Icon Left

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeWithIconLeft client:load />
</StyledDemoBlock>

### With Icon Right

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeWithIconRight client:load />
</StyledDemoBlock>

### With Spinner

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeWithSpinner client:load />
</StyledDemoBlock>

### As Link

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeAsLink client:load />
</StyledDemoBlock>

### Long Text

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeLongText client:load />
</StyledDemoBlock>

### Custom Colors

<StyledDemoBlock files={[{ label: "badge.tsx", code: demoCode }]}>
  <BadgeCustomColors client:load />
</StyledDemoBlock>

## Variants

| Variant       | Description                    |
| ------------- | ------------------------------ |
| `default`     | Primary background             |
| `secondary`   | Muted background               |
| `outline`     | Bordered with transparent fill |
| `destructive` | Red destructive styling        |
| `ghost`       | No background                  |
| `link`        | Text link appearance           |
