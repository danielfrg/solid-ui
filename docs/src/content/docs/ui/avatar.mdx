---
title: Avatar
description: Styled image element with a fallback for representing users.
---

import StyledDemoBlock from "../../../components/styled-demo-block.astro"
import demoCode from "../../../demos/styled/avatar.tsx?raw"
import {
  AvatarBasic,
  AvatarWithBadge,
  AvatarWithBadgeIcon,
  AvatarGroupExample,
  AvatarGroupWithCount,
  AvatarGroupWithIconCount,
} from "../../../demos/styled/avatar"

# Avatar

## Usage

Add the theme to your CSS:

```css
@import "tailwindcss";
@import "@danielfrg/solid-ui/theme.css";
```

Import and use the Avatar components:

```tsx
import { Avatar, AvatarImage, AvatarFallback } from "@danielfrg/solid-ui/avatar"
;<Avatar>
  <AvatarImage src="/avatar.png" />
  <AvatarFallback>CN</AvatarFallback>
</Avatar>
```

## Demo

### Basic

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarBasic client:load />
</StyledDemoBlock>

### With Badge

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarWithBadge client:load />
</StyledDemoBlock>

### With Badge Icon

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarWithBadgeIcon client:load />
</StyledDemoBlock>

### Group

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarGroupExample client:load />
</StyledDemoBlock>

### Group with Count

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarGroupWithCount client:load />
</StyledDemoBlock>

### Group with Icon Count

<StyledDemoBlock files={[{ label: "avatar.tsx", code: demoCode }]}>
  <AvatarGroupWithIconCount client:load />
</StyledDemoBlock>

## Parts

| Component          | Description                                    |
| ------------------ | ---------------------------------------------- |
| `Avatar`           | Root container (rounded, overflow hidden)      |
| `AvatarImage`      | The user image                                 |
| `AvatarFallback`   | Displayed when image is loading or unavailable |
| `AvatarBadge`      | Status indicator badge                         |
| `AvatarGroup`      | Container for grouped avatars                  |
| `AvatarGroupCount` | Counter for additional avatars                 |
